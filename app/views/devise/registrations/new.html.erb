<div class="flex flex-col gap-10 items-center justify-center w-full">
  <h2 class="text-3xl">Sign up</h2>

  <%= form_for(resource, as: resource_name, html: { 'data-turbo': 'false', class: 'w-full max-w-md' }, url: registration_path(resource_name))  do |f| %>
    <div class="flex flex-col items-center gap-3 w-full">
      <%= f.hidden_field :posts_counter, value: 0 %>    
      
      <%= render "devise/shared/error_messages", resource: resource %>    

      <div class="field w-full">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full" %>
      </div>

      <div class="field w-full">
        <%= f.label :name %><br />
        <%= f.text_field :name, class: "w-full" %>
      </div>

      <div class="field w-full">
        <%= f.label :photo %><br />
        <%= f.text_field :photo, class: "w-full" %>
      </div>

      <div class="flex flex-col gap-4 w-full">
        <%= f.label :biography %>
        <%= f.text_area :bio, rows: "5" , class: "text-xl w-full" %>
      </div>

      <div class="field w-full">
        <%= f.label :password %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "new-password", class: "w-full" %>
      </div>

      <div class="field w-full">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full" %>
      </div>

      <div class="actions mt-3 w-full max-w-sm">
        <%= f.submit "Sign up", class: "px-5 py-2.5 text-white bg-purple-700 hover:cursor-pointer hover:bg-purple-800 w-full" %>
      </div>  
    </div>    
  <% end %>

  <%= render "devise/shared/links" %>
</div>
